# TrailsApp-Microservice
The following repository is a microservice for a TrailsApp appplication, implemented with python as the server side code and a swagger.yml file serving as the front-end for the testing of API endpoints. It is also containerised in and deployed in Docker.
